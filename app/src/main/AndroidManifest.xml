<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- REQUIRED PERMISSION for voice recognition (microphone access) -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <!-- REQUIRED PERMISSION for internet access to fetch resources/APIs -->
    <uses-permission android:name="android.permission.INTERNET" />
    <!-- REQUIRED PERMISSION for foreground service (background monitoring) -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <!-- REQUIRED PERMISSION for notifications -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- REQUIRED PERMISSION for vibration -->
    <uses-permission android:name="android.permission.VIBRATE" />
    <!-- REQUIRED PERMISSION to wake device on alarm -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SmartHome"
        tools:targetApi="31">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Background Monitoring Service for Fire & Fault Alarms -->
        <service
            android:name=".AlarmMonitoringService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="dataSync" />
    </application>

</manifest>
